<%= form_for @detail, url: {action: "create"}, method: 'post' do |f| %>
  <%= f.collection_select(:material_id, @materials, :id, :name, {prompt: 'Seleccione un Material de la lista'}, {class: 'form-control'}) %>       
  <%= f.submit "Registrar", class: "btn btn-primary" %>
<% end %>