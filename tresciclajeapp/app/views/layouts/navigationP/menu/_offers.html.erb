<% if current_user.has_role?(:supplier) || current_user.has_role?(:admin) %>
<div class="panel-heading">
  <h4 class="panel-title">
    <a data-toggle="collapse"  href="#collapseThree">
      <div class="<%= 'active' if @location[:menu] == 'offers' %>">
        <i class="material-icons">add_shopping_cart</i>Ofertas</div>

    </a>
  </h4>
</div>
<div id="collapseThree" class="panel-collapse collapse in ">
  <ul class="list-group">
    <%= render 'layouts/navigation/suppliers/offers' %>
  </ul>
</div>
<% end %>
